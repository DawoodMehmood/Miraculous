import styles from './components.module.css';
function Header() {
    return (
        <header className={`${styles.header}`}>
            <h1>Admin Panel</h1>
        </header>
    );
  }

  export default Header;
